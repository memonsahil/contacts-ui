type contactType = {
    userId: string
    avatar: { uri: string }
    firstName: string
    lastName: string
    occupation: string
    bio: string
    phone: string
    email: string
    links: string[]
}

export default contactType
