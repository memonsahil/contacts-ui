type infoModalProps = {
    avatar: { uri: string }
    firstName: string
    lastName: string
    occupation: string
    phone: string
    email: string
    links: string[]
}

export default infoModalProps
